# 阅读室 V3.1 · 日常维护

## 1. 写一篇笔记

复制 `_templates/note.md` 到 `notes/`，例如 `notes/electron-transfer.md`。这个文件名只是后续文章命名示例，不表示电子转移内容已经建立。

```yaml
---
title: "你的笔记标题"
date: 2026-09-13
description: "一句话概括这篇笔记的问题。"
categories: [电子转移, 光谱]
lang: zh-CN
translation-key: electron-transfer
status: working
---
```

`categories` 控制分类；`description` 用于列表摘要。`status: working` 表示愿意公开、但持续修订的工作笔记。需要核对完成标识时用 `reviewed`，仅作存档时用 `archived`；网站不会替你判断内容是否正确。

模板初始有 `draft: true`。准备在网站展示时删除这一行或改成 `false`。保留 `draft: true` 的笔记会从本项目公开索引排除，并交给 Quarto 的草稿机制处理。**这不是隐私保护：若源文件已经进入公开 GitHub 仓库，别人仍可能看到源码。** 机密内容应留在本地未提交目录或另行管理，不要用草稿标签代替权限控制。

`.md` 和 `.qmd` 都支持。不要在相同目录同时使用 `example.md` 和 `example.qmd`，它们会产生相同输出文件。不要复制演示文件的 `demo: true` 到正式笔记；也不要未经判断就把状态写成 `reviewed`。

## 2. 保留文件之间的关系

使用标准相对 Markdown 链接：

```markdown
[进一步推导](02-derivation.md#result)
[LaTeX 源文件](../files/objective.tex)
[交互式结果](../files/response-surface.html)
![图像说明](../assets/your-image.png)
```

最后一行的 `your-image.png` 是命名示例，需要替换成实际存在的图片。链接是索引依据；不需要手写另一份关系表。构建时会提取标准链接，生成左侧关联资料和文章底部的反向链接。

需要直接定位章节时，给标题一个稳定锚点：

```markdown
## 推导结论 {#result}
```

原来的目录结构应尽量保留。不要改成电脑路径 `C:\Users\...`。Obsidian 的 `[[双链]]`、HTML 内脚本动态产生的跳转、全文中的纯文字文件名，均不自动等同于标准链接；迁移时需要转换或显式补充链接。

同标签页内可沿多层路径逐级返回，并恢复来源页面的滚动位置。直接从外站访问而没有阅读路径时返回目录。新标签页与新窗口保持浏览器原生行为，不强行跨窗口追踪。附件 HTML 内框自身的导航不计入外层笔记路径。清除浏览器数据或禁用存储会影响路径恢复。

## 3. 英文版怎样增加

中文文件与英文文件分开保存，使用相同的 `translation-key`：

```text
notes/electron-transfer.md       # lang: zh-CN
notes/electron-transfer.en.md    # lang: en
```

两者都写 `translation-key: electron-transfer`，但 `title`、`description` 和正文分别使用各自语言。英文稿不是只改一个标题；科学表述和术语应逐段核对。

中文版 `## 模型 {#model}` 对应英文版 `## Model {#model}`：标题可以翻译，`model` 这样的锚点保持一致。切换语言时会尽量落回对应章节及相对阅读位置。翻译内容长短差异很大或缺少共享锚点时，不保证逐像素相同。

E / 中是实际切换键。有配对文章就进入对应正文，不把切换当作打开新的关联文件；没有配对文章则保留原文、显示缺失译稿提示，并切换界面语言。附件维持原语言，不自动改写 HTML、PDF 或数据。

目录每组只显示一篇，按当前语言优先选择译稿。现有分类在 `assets/site.js` 的 `topicNames` 中有英译映射；新增中文分类会保持原名，补充映射后才显示英文分类名。

## 4. 字号、字重与左侧目录

默认 22 px / 500；字号可在 18–28 px 之间调整，字重为常规 400、适中 500、加粗 600。大粗 A 与小 A 快捷档为 24/600 和 20/400。三档字重可与任意字号组合。

字重变化主要用于正文。数学符号不被全局机械加粗，保留文稿中 `\mathbf` 等有语义的显式定义。具体字形和中间字重取决于阅读者的系统字体；项目不附带字体文件，也不要求下载字体。

单独点击目录收起按钮，会给正文更多空间。「专注阅读」进一步收起非必要顶部导航，保留阅读设置、语言键与返回键。木色轨道表示开启；拨片本身仍微凸。

移动端目录为可展开抽屉。长公式、表格和代码各自可以横向滚动，不把整页撑宽。笔记底部独立手势区支持右滑返回、左滑去第一个站内关联链接；正文选择文字和横向表格不被接管。系统减少动态效果的偏好会被尊重。

## 5. 科学公式与附件

当前使用 Quarto 的 `html-math-method: mathml`。已用样例检查基础分式、求和、矩阵、上下标、bra-ket 写法、微分符号与化学式排版；**没有根据尚未收到的原稿检查自定义宏、物理/化学宏包或复杂光谱图的完整兼容性**。

完整 LaTeX 论文和 `\usepackage` 不能直接当作网页公式。源码附件保留原文件；网站只展示文本，不编译整个论文。真实文件请使用 UTF-8 编码。

遇到无法转换的宏，优先逐条适配原稿；也可按 Quarto 官方说明改用 MathJax，但需要重新核对脚本加载、网络和模板兼容性，不能把切换后的结果视为本次已测试范围。

HTML 使用隔离 iframe；PDF 显示取决于浏览器内置预览；文本、LaTeX、CSV、代码和 Notebook 原文使用 UTF-8 显示，文本预览上限 2 MB。Notebook 和 Python 不自动执行。打开新的窗口后可以看到原文件，但不要直接打开不可信的 HTML。

## 6. 更新和发布

修改内容后完整构建：

```powershell
quarto render
quarto preview
```

新建、移动、删除或调整分类以后，停止预览再重新启动，避免当前页面与别的页面的关联索引不一致。不要编辑 `_preview/` 或 `_site/` 里的 HTML；源文件才是以后更新的依据。

发布使用包内的 GitHub Actions 工作流。设置 Pages 来源为 GitHub Actions，不与旧的 gh-pages 分支发布混用。详细命令在 `发布到GitHub.md`。

## 7. 公开范围与素材

`files/` 和 `assets/` 会复制到公开网站；不要放密钥、未授权文献、未脱敏数据或未准备公开的实验资料。`_drafts/` / `_private/` 在本包默认忽略，但已经被 Git 跟踪的文件不会因为新增忽略规则而自动从历史中消失。

`assets/courtyard-reference.png` 沿用上版用户提供的风格参考图，首页已不再使用它。演示笔记仍可能引用该图；正式公开前确认可使用，或替换/移除相关图片和链接。所有演示笔记、CSV、HTML 与 TeX 示例可以在加入真实内容后替换。

官方参考：
- https://quarto.org/docs/output-formats/html-basics.html
- https://quarto.org/docs/websites/website-drafts.html
- https://quarto.org/docs/extensions/lua-api.html

## V3.1 配色和紧凑返回键

`assets/site.css` 顶部的两组变量控制浅色与深色。深色表面是 V2 RGB 通道乘以 0.9；浅色表面按 90% 原色与 10% 暖米白混合。`_checks/palette.json` 记录原色和结果，不使用屏幕级 brightness 或滤蓝效果。

右上角浅／深主题与 E／中常驻。`assets/boot.js` 在绘制前应用保存的主题，`assets/site.js` 继续处理 V3 的阅读设置和关联导航。V3 的字号与字重可自动迁移到 V3.1 的浏览器偏好中；这些设置并不会上传到服务器。

返回键可见面只有箭头和“返回”或 Back，实际来源文件存放在 `title` 与 `aria-label`。桌面尺寸 64 px，手机 52 px；不要只修改宽度而保留旧版的大 `min-height`。手机正文右侧的安全留白与该尺寸配套。

源代码中的 `notes/*.en.md` 为原 V3 演示译稿。没有新增你的英文稿；真实笔记的译稿由你后续通过相同 `translation-key` 配对添加。
