# linminxi007 · Reading Room V3.1

这是以 V3 为基础合并导航、明暗与按钮设计后的**单一完整版本**。不需要再叠加 V2 文件或更新包。

## 打开网站

先保留原网站文件夹，将本 ZIP 解压到新文件夹。ZIP 内没有额外套一层项目目录。
在 VS Code 中打开**直接包含 `_quarto.yml` 和 `index.qmd` 的文件夹**，运行：

```powershell
quarto render
quarto preview
```

也可以双击 `CHECK-RENDER.cmd` 和 `START-PREVIEW.cmd`。它们会自动进入正确目录。
旧预览先按 `Ctrl+C` 停止；新版仍显示旧外观时，在浏览器按 `Ctrl+F5`。
不要在 `_preview` 或上一级 Downloads 中运行 Quarto。

## V3.1 包含的调整

以 V3 的首页、长文布局、左侧可收目录、关联链接、返回路径及 AA 设置为基础。
导航合入房子图标、笔记、分类、关联文件、关于与搜索。左上角仍只有 linminxi007。

浅色表面以 V2 原色与暖米白 #ffe4bf 按 90:10 混合；深色表面以 V2 原色与黑色按 90:10 混合。
这是界面色值调整，不是显示器蓝光辐射测量；不对文字、图片、图表或公式套用全屏滤色。
右上角浅／深色开关常驻。深色和英文开关启用时显示木色轨道。无玻璃和毛玻璃模糊。

返回用 01 深凹、跳转用 05 微凸、打开与二级菜单用 02 深槽。
返回缩为桌面 64 × 64 px、手机 52 × 52 px 的圆角方块，只显示图标与“返回”（英文界面显示 Back）。
手机正文右侧保留小幅安全间距，避免按钮遮住文字；考虑屏幕底部安全区。
文件名移到 title 与 aria-label，不再撑大按钮。

保留 V3 的完整 AA 面板：常规／大字清晰快捷档、18–28 px 字号滑杆、400／500／600 三档字重、文字预览、专注阅读和恢复默认。
默认仍是 22 px / 500。E / 中是语言版本入口；不添加或自动翻译你的科研正文。
原 V3 包中的演示译稿保留；没有配对译稿时明确提示，并保留原文。

## 保留已有内容

这个包继承的是上次交付 V3 中的演示内容，不包含你电脑上之后新写的文件。
把自己的 `notes/`、`files/` 和图片按原相对路径迁入；同名文件先核对，不要直接覆盖。
不要把原 V3 的 `assets/site.css`、`assets/site.js` 或 `_theme` 覆盖回本版，否则会恢复旧控件。
GitHub 发布方式与 V3 相同，见 `发布到GitHub.md`。

## 验证范围

`_preview/` 是使用本版模板、过滤器、CSS 与脚本生成的 Pandoc 静态排版快照，不是 Quarto 输出。
可直接打开 `_preview/index.html` 查看外观；file:// 打开时部分附件读取可能被浏览器限制。
日常请使用 `quarto preview`。

测试范围及环境限制见 `验证说明.md`；本包不把 Pandoc 或模拟导航检查写成 Quarto／GitHub 实际部署成功。
