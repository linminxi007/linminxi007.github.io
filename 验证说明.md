# V3.1 交付验证记录

## 本次实际完成

用 pandoc 3.1.11.1，对本包的 `_theme/page.html`、`_theme/site.lua` 和笔记源文件进行渲染，生成 14 个页面。
检查 389 个站内资源／链接目标，其中含 93 个锚点；未发现缺失目标或重复页面 ID。
CSS 语法检查和 JavaScript `node --check` 通过。

离线 Chromium 共通过 98 个断言：桌面 51 个，响应式 47 个。
覆盖 320、360、390、540、720、768、940、1024、1440 px 模拟视口，检查导航在屏幕内、移动端 52px 返回键与文字列分离、AA 面板不越界、目录抽屉以及宽表格滚动。
另检查配色数值、V3 字体设置、明暗切换、英文稿配对／缺失提示、分类、搜索、多级返回、滚动恢复、旧偏好迁移与无效偏好归一化。

截图在 `_checks/previews/`，均来自交付 HTML/CSS 的浏览器绘制，不是概念图。

## 测试边界

当前环境未提供 Quarto CLI，尝试获取后仍无法运行。因此本次**没有执行 `quarto render` 或 `quarto preview`，也没有替你部署 GitHub Pages**。

浏览器环境阻止 URL 导航，包含本地 HTTP。浏览器检查采用离线 `set_content` 加载，并用显式的测试适配器替代 location、history、localStorage、sessionStorage、fetch；页面结构、CSS、控件逻辑与关联导航算法使用本包源码。
多级跳转、偏好持久化与附件读取属于模拟检查，不等同于真实浏览器跨 URL／跨标签页或公网部署测试。手机尺寸为模拟视口，未作实体手机测试。HTML 附件内部脚本没有被当作真实网站运行来验证。

独立 Pandoc 对中文元数据给出了缺失 `translations/zh.yaml` 和 Abstract 翻译的警告，全部页面仍以退出码 0 完成；详见 `render-results.json`。此警告不代表已经验证 Quarto 内置 Pandoc 的行为。

## 本地首次核对

在含 `_quarto.yml` 的项目根目录执行 `quarto render`，完成后用 `quarto preview` 打开。测试一条真实路径：

首页 → 阅读示例 → 推导笔记 → LaTeX 附件 → 返回 → 返回。

再测试 AA、浅／深色、E／中与手机宽度。首次构建结果以你电脑上的 Quarto 输出为准。

## 原始记录

`_checks/render-results.json`、`static-checks.json`、`desktop-tests.json`、`responsive-tests.json`、`palette.json`。
测试脚本放在 `_checks/scripts/`，只用于可选的开发检查，不是运行网站的必需依赖。
